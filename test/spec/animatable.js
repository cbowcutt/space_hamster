eval(fs.readFileSync('../rectangle.js')+'');
eval(fs.readFileSync('../animatable.js')+'');

describe('Character', function() {
	var player = new PlayerSprite()
	it("should have a rectangle", function() {

	});

	it("should be animatable", function() {

	});

	it("should move when given commands", function() {

	});
});
